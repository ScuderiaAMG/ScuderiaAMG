# ============================================================
# Conda 环境配置 — Battery CNN Aging Classification + RUL
# 目标硬件: NVIDIA RTX 4060 Laptop GPU 8GB
# 系统: Windows 11, Anaconda
# ============================================================
# 创建环境:
#   conda create -n batcnn python=3.11 -y
#   conda activate batcnn
#   pip install -r env.txt
# ============================================================

# --- PyTorch 2.5.x (CUDA 12.4, RTX 4060 compute capability 8.9) ---
torch>=2.5.0,<2.7.0
torchvision>=0.20.0
torchaudio>=2.5.0

# --- 科学计算 ---
numpy>=1.26.0,<2.0.0
scipy>=1.13.0
scikit-learn>=1.5.0
pandas>=2.2.0

# --- ONNX 导出与部署 ---
onnx>=1.16.0
onnxruntime>=1.18.0
onnxruntime-gpu>=1.18.0

# --- 可视化与监控 ---
tensorboard>=2.17.0
matplotlib>=3.9.0

# --- 数据处理 (NASA PCoE .mat / CALCE .xlsx) ---
h5py>=3.11.0
openpyxl>=3.1.0

# --- 开发工具 ---
tqdm>=4.66.0
