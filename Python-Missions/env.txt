# ============================================================================
# Conda Environment for Python-Missions (01_basics/ ~ 12_math_and_optimization/)
# Platform: Windows 11, Anaconda
# ============================================================================
#
# Create the environment:
#   conda create -n pymissions python=3.10 -y
#   conda activate pymissions
#
# Then install packages in order:
#   conda install numpy pandas scipy -c conda-forge -y
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#
# Or use the one-liner below after creating + activating the env.
# ============================================================================

# --- environment name ---
# pymissions

# --- channels ---
# conda-forge (recommended for up-to-date Windows packages)
# pytorch      (only if installing PyTorch via conda; pip is preferred on Windows)

# --- Python version ---
python=3.10

# --- Core scientific packages (conda) ---
numpy>=1.24,<2.0
pandas>=2.0
scipy>=1.10

# --- Deep learning (pip — more reliable on Windows than conda PyTorch) ---
# Run this separately after conda installs:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#
# CUDA 12.1 build. If you don't have an NVIDIA GPU, use the CPU version instead:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

torch>=2.0

# ============================================================================
# Quick Setup (copy-paste into Anaconda Prompt):
# ============================================================================
#
#   conda create -n pymissions python=3.10 -y
#   conda activate pymissions
#   conda install numpy pandas scipy -c conda-forge -y
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#
# ============================================================================
# Package Usage Map:
# ============================================================================
# numpy   — 04_data_science, 05_machine_learning, 06_reinforcement_learning,
#           08_computer_vision, 09_nlp, 12_math_and_optimization
# pandas  — 04_data_science
# torch   — 05_machine_learning (CNN, Transformer),
#           06_reinforcement_learning (DQN, Policy Gradient, PPO, MCTS)
# scipy   — 08_computer_vision (optional), 12_math_and_optimization
# ============================================================================
