两个文件已生成完毕。以下是总结：
                                                                                                                                                              ---
  Q1.c — 用户菜单权限管理系统                                                                                                                               
  数据结构设计：                                                                                                                                              - 菜单项（MenuItem）：ID、父级ID、名称、动作描述，构成2级树形层级（4个一级菜单，10个二级菜单项）                                                            - 角色（Role）：系统管理员、操作员、普通用户                                                                                                                - 用户（User）：admin/admin123、operator/oper123、user/user123，分别对应三种角色                                                                            - 权限（Permission）：visible（可见性）+ enabled（可操作性）的细粒度控制                                                                                                                                                                                                                                                图形界面（Win32 API）：
  - 登录窗口 → 选择用户名、输入密码
  - 主窗口左侧 TreeView 树形控件展示可见菜单（按角色过滤）
  - 双击叶子菜单项：有权限则弹出操作窗口，无权限则弹出警告提示
  - 底部状态栏显示当前用户和角色

  ---
  Q2.c — 路网最短路径计算系统

  算法设计：
  - 随机生成 55 个节点的连通路网，节点网格布局 + 随机抖动
  - Union-Find 保证图连通性，边权 = 欧几里得距离/8 + 随机偏移
  - Dijkstra 算法求两点间最短路径
  - 途径点支持：将 A→WP₁→WP₂→...→B 拆成多段 Dijkstra 串联

  图形界面（Win32 API + GDI）：
  - 上方大面积 GDI 绘图区：灰色边线 + 权重标签，节点圆形标注
  - 最短路径以红色粗线高亮，起点绿色、终点蓝色、途径点橙色
  - 下方控制面板：起点/终点/途径点下拉框，添加/移除途径点按钮
  - "计算最短路径"大按钮，底部显示路径序列和总权重

  ---
  编译命令：
  gcc Q1.c -o Q1.exe -lcomctl32 -lgdi32
  gcc Q2.c -o Q2.exe -lcomctl32 -lgdi32

  当前环境未安装 gcc，你需要在安装了 MinGW 或 MSVC 的机器上编译运行。

✻ Worked for 12m 2s