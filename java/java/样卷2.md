# Java程序设计 样卷2

**总分：100分 | 考试时间：120分钟**

---

## 一、阅读题（共40分）

阅读下列程序，写出程序的运行结果或回答相关问题。

### 1.1（10分）阅读以下程序，写出运行结果。

```java
class Parent {
    static {
        System.out.println("Parent static block");
    }
    
    {
        System.out.println("Parent instance block");
    }
    
    public Parent() {
        System.out.println("Parent constructor");
    }
}

class Child extends Parent {
    static {
        System.out.println("Child static block");
    }
    
    {
        System.out.println("Child instance block");
    }
    
    public Child() {
        System.out.println("Child constructor");
    }
}

public class TestInit {
    public static void main(String[] args) {
        new Child();
        System.out.println("---");
        new Child();
    }
}
```

### 1.2（10分）阅读以下程序，写出运行结果。

```java
class Shape {
    public void draw() {
        System.out.println("Drawing Shape");
    }
    
    public void erase() {
        System.out.println("Erasing Shape");
    }
}

class Circle extends Shape {
    public void draw() {
        System.out.println("Drawing Circle");
    }
    
    public void erase() {
        System.out.println("Erasing Circle");
    }
}

class Square extends Shape {
    public void draw() {
        System.out.println("Drawing Square");
    }
}

public class TestShapes {
    static void drawOneShape(Shape s) {
        s.draw();
    }
    
    public static void main(String[] args) {
        Shape[] shapes = new Shape[3];
        shapes[0] = new Shape();
        shapes[1] = new Circle();
        shapes[2] = new Square();
        
        for (Shape s : shapes) {
            drawOneShape(s);
            s.erase();
        }
    }
}
```

### 1.3（10分）阅读以下程序，写出运行结果。

```java
interface Employee {
    void work();
    void raiseSalary();
}

abstract class Programmer implements Employee {
    String name;
    String id;
    double pay;
    
    public Programmer(String name, String id, double pay) {
        this.name = name;
        this.id = id;
        this.pay = pay;
    }
    
    public abstract void work();
    
    public void raiseSalary() {
        pay = pay * 1.1;
        System.out.println(name + " 涨薪了，现薪资：" + pay);
    }
}

class TeamLeader extends Programmer {
    public TeamLeader(String name, String id, double pay) {
        super(name, id, pay);
    }
    
    public void work() {
        System.out.println(name + "（组长）在分配任务");
    }
    
    public void raiseSalary() {
        pay = pay * 1.2;
        System.out.println(name + "（组长）涨薪了，现薪资：" + pay);
    }
}

public class TestEmployee {
    public static void main(String[] args) {
        Programmer p = new TeamLeader("张三", "T1001", 8000);
        p.work();
        p.raiseSalary();
    }
}
```

### 1.4（10分）阅读以下程序，回答问题。

```java
class Outer {
    private int outerVar = 10;
    
    class Inner {
        private int innerVar = 20;
        
        public void display() {
            System.out.println("outerVar = " + outerVar);
            System.out.println("innerVar = " + innerVar);
        }
    }
    
    public void createInner() {
        Inner in = new Inner();
        in.display();
        System.out.println("Access innerVar: " + in.innerVar);
    }
    
    static class StaticInner {
        public void show() {
            System.out.println("Static inner class method");
        }
    }
}

public class TestInner {
    public static void main(String[] args) {
        Outer out = new Outer();
        out.createInner();
        
        Outer.Inner inner = out.new Inner();
        inner.display();
        
        Outer.StaticInner staticInner = new Outer.StaticInner();
        staticInner.show();
    }
}
```

**问题**：
（1）写出程序运行结果。
（2）说明成员内部类和静态内部类在使用上有什么区别？

---

## 二、改错题（共20分）

找出下列程序中的错误，说明错误原因并给出修改办法。

### 2.1（10分）

```java
abstract class Vehicle {
    public abstract void run();
    
    public void stop() {
        System.out.println("Vehicle stopped");
    }
}

class Car extends Vehicle {
    public void run() {
        System.out.println("Car running");
    }
}

public class TestVehicle {
    public static void main(String[] args) {
        Vehicle v = new Vehicle();
        v.run();
        v.stop();
    }
}
```

### 2.2（10分）

```java
interface Calculator {
    int add(int a, int b);
    int subtract(int a, int b);
}

class SimpleCalculator implements Calculator {
    public int add(int a, int b) {
        return a + b;
    }
}

public class TestCalculator {
    public static void main(String[] args) {
        Calculator calc = new Calculator();
        System.out.println("5 + 3 = " + calc.add(5, 3));
    }
}
```

---

## 三、写代码题（共40分）

请根据要求编写完整的Java程序。

### 3.1（15分）图形继承体系

编写程序：
- 定义抽象类 `Shape`，包含抽象方法 `double getArea()` 和 `double getPerimeter()`
- 定义子类 `Circle`，包含属性 `double radius`，构造方法初始化半径，实现抽象方法
- 定义子类 `Rectangle`，包含属性 `double width` 和 `double height`，构造方法初始化宽高，实现抽象方法
- 在 `main` 方法中，创建 `Circle` 对象（半径=5）和 `Rectangle` 对象（宽=4，高=6），通过 `Shape` 引用调用方法，输出各自的面积和周长
- （π取3.14）

### 3.2（12分）自定义异常

编写程序：
- 定义一个自定义异常类 `AgeException`，继承 `Exception`，包含有参构造方法传递异常消息
- 定义一个类 `Person`，包含私有属性 `String name` 和 `int age`
- `Person` 的构造方法中，如果年龄小于0或大于150，抛出 `AgeException`，异常消息为"年龄不合法"
- 在 `main` 方法中，测试创建合法的 `Person` 对象和非法的 `Person` 对象，并用 `try-catch` 捕获异常并打印堆栈信息

### 3.3（13分）静态工厂模式

编写程序：
- 定义一个类 `Product`，包含私有属性 `String name` 和 `double price`
- 私有构造方法 `Product(String name, double price)` 初始化属性
- 提供一个静态方法 `public static Product createProduct(String name, double price)` 作为工厂方法，在其中进行价格校验：如果 `price < 0`，返回 `null`；否则创建并返回 `Product` 对象
- 提供 `void printInfo()` 方法打印产品信息
- 在 `main` 方法中，使用工厂方法创建产品并测试

---
