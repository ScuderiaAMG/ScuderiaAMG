# 08 图形化用户界面（GUI）

---

## 一、本章概述

本章主要涵盖以下内容：

- **Java GUI概述**：JFC、AWT、Swing
- **基于Swing的GUI构造方法**
- **AWT事件处理模型**
- **Swing组件**

---

## 二、Java基础类库（JFC）

### 2.1 JFC概述

**JFC（Java Foundation Classes）** 是Java基础类库，包括开发GUI所需的组件和服务，包含5个部分的API：

| 模块 | 说明 |
|------|------|
| **AWT** | 抽象窗口工具集，包括建立GUI的各种组件与事件处理机制 |
| **Java 2D** | 一套图形API，提供高级二维图形图像处理类，扩展了java.awt和java.awt.image类，提供丰富的绘图风格 |
| **Accessibility** | 辅助工具，支持非传统输入和输出的应用程序（如屏幕阅读器、屏幕放大器、语音处理等） |
| **Drag & Drop** | 拖放功能，提供Java和本地应用程序之间的互操作性，在Java和非Java应用程序之间交换数据 |
| **Swing** | JFC模块的**重点**，用于基于窗口的应用程序开发，提供丰富的组件和工作框架，指定GUI如何独立于平台地展现其视觉效果 |

---

## 三、抽象窗口工具集（AWT）简介

### 3.1 AWT设计目标与实现方法

- **设计目标**：具有**独立于平台的API**，但同时保留每个平台的界面显示风格 **L&F（Look & Feel）**
- **实现方法**：定义各种组件（Components）类平台独立的API，然后利用特定于平台的各种类的实现（称为**对等组件，Peers**）提供具有特定平台风格的L&F

**架构示意**：
```
平台无关API → Button 类
                ↓
特定于平台的实现 → ButtonPeer (Windows) / ButtonPeer (MacOS) / ButtonPeer (Motif)
```

### 3.2 AWT主要类及层次关系

```
Object
  ├── BorderLayout
  ├── AWTEvent
  ├── FlowLayout
  ├── GridBagLayout
  ├── MenuComponent
  │     └── MenuBar
  │           └── MenuItem
  └── Component
        ├── Button
        └── Container
              ├── Panel
              │     └── Applet
              ├── ScrollPane
              ├── Window
              │     ├── Dialog
              │     └── Frame
              └── ...
```

---

## 四、Swing简介

### 4.1 Swing的特点

| 特点 | 说明 |
|------|------|
| **全部Java实现** | Swing中的所有组件全部用Java语言实现（**轻量级 Lightweight**） |
| **分离模型** | 组件及与组件相关的数据模型分离 |
| **可设置L&F** | 可设置的组件外观感觉（Look & Feel） |
| **更丰富组件** | 与AWT相比提供更丰富的GUI组件，引入新的特征和更丰富的功能 |

### 4.2 Swing相比AWT的增强功能

- 高层组件集合：如表格 `JTable`、树 `JTree`
- 都实现了 `Accessible` 接口
- 支持**键盘代替鼠标**
- 设置**边框**
- 使用**图标**

### 4.3 Swing组件类层次

```
java.awt.Container
  └── javax.swing.JComponent
        ├── JTextComponent
        │     ├── JTextArea
        │     ├── JTextField
        │     │     └── JPasswordField
        │     └── JEditorPane
        │           └── JTextPane
        ├── AbstractButton
        │     ├── JToggleButton
        │     │     ├── JCheckBox
        │     │     └── JRadioButton
        │     ├── JButton
        │     └── JMenuItem
        │           ├── JRadioButtonMenuItem
        │           ├── JCheckBoxMenuItem
        │           └── JMenu
        ├── JPanel
        ├── JComboBox
        ├── JLabel
        ├── JTree
        └── ...
```

---

## 五、基于Swing的GUI设计步骤

### 5.1 基本步骤

1. 引入Swing包及其它程序包
2. 选择GUI的外观风格L&F
3. 创建并设置窗口容器
4. 创建与添加Swing组件
5. 显示顶层容器，将整个GUI显示出来

### 5.2 HelloWorld示例

```java
import javax.swing.*;

public class HelloWorldSwing {
    public static void main(String[] args) {
        JFrame frame = new JFrame("HelloWorldSwing");
        final JLabel label = new JLabel("Hello World!");
        frame.add(label);
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.setSize(200, 70);
        frame.setVisible(true);
    }
}
```

---

## 六、Swing中的Look & Feel

### 6.1 三种L&F效果

| L&F类型 | 说明 |
|---------|------|
| CDE/Motif L&F | Unix/Motif风格 |
| Windows L&F | Windows平台风格 |
| Java L&F (Metal) | Java跨平台默认风格 |

### 6.2 设置L&F

使用 `UIManager.setLookAndFeel()` 方法进行设置：

| 参数 | 对应L&F |
|------|---------|
| `UIManager.getCrossPlatformLookAndFeelClassName()` | Java Look & Feel |
| `UIManager.getSystemLookAndFeelClassName()` | 当前平台的Look & Feel |
| `"javax.swing.plaf.metal.MetalLookAndFeel"` | Java Look & Feel |
| `"com.sun.java.swing.plaf.windows.WindowsLookAndFeel"` | Windows Look & Feel |
| `"com.sun.java.swing.plaf.mac.MacLookAndFeel"` | Mac OS Look & Feel |

---

## 七、常用容器

### 7.1 容器概述

- 组成GUI的组件（如按钮、标签等）不能独立使用，**必须放在容器内**
- 容器本身也是一个组件，具有组件的所有性质，另外还具有**容纳其它组件**的功能
- 所有的组件都可以通过 `add()` 方法向容器中添加
- 在Swing中，常用的三种类型的容器是：**JFrame**、**JPanel**、**JApplet**

### 7.2 三层容器层次结构

Swing GUI形成 **顶层容器 → 中间层容器 → 基本组件** 的层次包容关系：

| 层次 | 说明 | 常用组件 |
|------|------|----------|
| **顶层容器** | 提供组件需要的绘制与事件处理功能，应用必须至少有一个 | JFrame、JDialog、JApplet |
| **中间层容器** | 由通用容器构成，主要简化组件布局 | JPanel、JScrollPane、JTabbedPane |
| **基本组件** | 直接向用户展示信息或获取用户输入 | JButton、JLabel、JTextField等 |

### 7.4 JFrame（顶层容器）

最常用的顶层容器，一般作为Java Application的**主窗口**。

**向JFrame添加组件的三种方式**：

| 方式 | 说明 |
|------|------|
| `frame.getContentPane().add(component)` | 获取JFrame的内容面板，再用add()方法加入组件 |
| `frame.setContentPane(panel)` | 建立JPanel等中间容器，添加组件到中间容器，再设置为内容面板 |
| `frame.add(child)` | JDK 1.5+后add()方法被重写，自动转交给内容面板 |

**JFrame示例**：
```java
import java.awt.*;
import javax.swing.*;

public class JFrameDemo {
    public static void main(String s[]) {
        // 指定使用当前的Look&Feel装饰窗口（必须在创建窗口前设定）
        JFrame.setDefaultLookAndFeelDecorated(true);
        // 创建并设定关闭窗口操作
        JFrame frame = new JFrame("JFrameDemo");
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        // 创建一个JLabel并加到窗口中
        JLabel emptyLabel = new JLabel("");
        emptyLabel.setPreferredSize(new Dimension(175, 100));
        frame.add(emptyLabel, BorderLayout.CENTER);
        // 显示窗口
        frame.pack();
        frame.setVisible(true);
    }
}
```

### 7.5 JPanel（通用容器）

JPanel是存放轻型组件的**通用容器**，缺省情况下是**透明**的，其对象可作为顶层容器的Content Pane使用。

**JPanel示例**：
```java
import java.awt.*;
import javax.swing.*;

public class FrameWithPanel extends JFrame {
    public FrameWithPanel(String str) {
        super(str);
    }

    public static void main(String args[]) {
        FrameWithPanel fr = new FrameWithPanel("Hello !");
        fr.setSize(200, 200);
        fr.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        JPanel pan = new JPanel();
        pan.setSize(200, 100);
        pan.setBackground(Color.yellow);
        pan.setLayout(new GridLayout(2, 1));
        pan.add(new JButton("确定"));
        fr.setContentPane(pan);
        fr.setVisible(true);
    }
}
```

---

## 八、布局管理器（Layout Manager）

### 8.1 布局管理器概述

为了实现跨平台的特性并获得动态的布局效果，Java在容器（如JFrame、JPanel）中设置了**布局管理器（Layout Manager）** 负责对容器内的组件进行布局。

**核心特点**：
- 容器中组件的布局通常由Layout Manager控制
- Layout Manager负责决定容器的布局策略及容器内组件的排列顺序、大小和位置，以及当窗口移动或调整大小后组件如何变化
- 每个容器都有一个**默认的Layout Manager**，可通过调用 `setLayout()` 方法改变

### 8.2 Java提供的布局管理器

| 布局管理器 | 类型 | 说明 |
|-----------|------|------|
| **FlowLayout** | 流式 | 从左到右、从上到下逐行摆放 |
| **BorderLayout** | 边界 | 分5个区（North/South/West/East/Center） |
| **GridLayout** | 网格 | n行×m列同样大小的网格单元 |
| **CardLayout** | 卡片 | 组件像一系列卡片叠放，一次只能看到最上面的 |
| **GridBagLayout** | 网格包 | 不需要组件尺寸一致，容许组件扩展到多行多列 |
| **BoxLayout** | 箱式 | Swing新增，将组件垂直或水平摆放 |

### 8.3 FlowLayout

**特点**：组件采用从左到右，从上到下逐行摆放。**Panel和Applet默认布局**。

**构造方法**：
```java
setLayout(new FlowLayout(int align, int hgap, int vgap));
// FlowLayout.LEFT / FlowLayout.CENTER / FlowLayout.RIGHT
// 缺省是居中
```

**示例**：
```java
import java.awt.*;
import javax.swing.*;

public class FlowLayoutWindow extends JFrame {
    public FlowLayoutWindow() {
        setLayout(new FlowLayout());
        add(new JLabel("Buttons:"));
        add(new JButton("Button 1"));
        add(new JButton("2"));
        add(new JButton("Button 3"));
        add(new JButton("Long-Named Button 4"));
        add(new JButton("Button 5"));
    }
    public static void main(String args[]) {
        FlowLayoutWindow window = new FlowLayoutWindow();
        window.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        window.setTitle("FlowLayoutWindow Application");
        window.pack();  // 窗口大小设置为适合组件最佳尺寸与布局所需空间
        window.setVisible(true);
    }
}
```

### 8.4 BorderLayout

**特点**：分5个区（North、South、West、East、Center）。**Window、Dialog和Frame默认布局**。

**构造与使用**：
```java
setLayout(new BorderLayout(int hgap, int vgap));
add(new JButton("Center"), "Center");
```

**示例**：
```java
import java.awt.*;
import javax.swing.*;

public class BorderLayoutWindow extends JFrame {
    public BorderLayoutWindow() {
        setLayout(new BorderLayout());
        add(new JButton("North"), "North");
        add(new JButton("South"), "South");
        add(new JButton("East"), "East");
        add(new JButton("West"), "West");
        add(new JButton("Center"), "Center");
    }
    public static void main(String args[]) {
        BorderLayoutWindow window = new BorderLayoutWindow();
        window.setTitle("BorderWindow Application");
        window.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        window.pack();
        window.setVisible(true);
    }
}
```

### 8.5 GridLayout

**特点**：把容器分成 **n行×m列** 同样大小的网格单元，每个单元可充满一个组件。组件从左到右、从上到下填充。

**构造方法**：
```java
setLayout(new GridLayout(int rows, int cols, int hgap, int vgap));
// rows或cols可取0，表示任意数目的行或列；先满足行的限制
```

**示例**：
```java
import java.awt.*;
import javax.swing.*;

public class GridLayoutWindow extends JFrame {
    public GridLayoutWindow() {
        setLayout(new GridLayout(3, 2));
        add(new JButton("Button 1"));
        add(new JButton("2"));
        add(new JButton("Button 3"));
        add(new JButton("Long-Named Button 4"));
        add(new JButton("Button 5"));
        add(new JButton("6"));
        add(new JButton("找茬"));  // 行优先
    }
    public static void main(String args[]) {
        GridLayoutWindow window = new GridLayoutWindow();
        window.setTitle("GridWindow Application");
        window.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        window.pack();
        window.setVisible(true);
    }
}
```

### 8.6 CardLayout

**特点**：把组件像**一系列卡片**一样叠放，一个时刻只能看到最上面的。

**构造方法**：
```java
setLayout(new CardLayout(int hgap, int vgap));
```

**导航方法**：`first()`, `previous()`, `next()`, `last()`, `show()`

**添加组件**：
```java
public Component add(String name, Component comp);
```

**示例**：
```java
import java.awt.*;
import java.awt.event.*;
import javax.swing.*;

public class CardLayoutWindow extends JFrame implements ActionListener {
    JPanel cards;
    CardLayout CLayout = new CardLayout();

    public CardLayoutWindow() {
        setLayout(new BorderLayout());

        // 创建摆放"卡片切换"按钮的panel,并添加到Frame中
        JPanel cp = new JPanel();
        JButton bt = new JButton("卡片切换");
        bt.addActionListener(this);
        cp.add(bt);
        add("North", cp);

        // 创建承放多个卡片的Panel，设置为CardLayout
        cards = new JPanel();
        cards.setLayout(CLayout);

        // 创建第一个panel及其组件
        JPanel p1 = new JPanel();
        p1.add(new JButton("Button 1"));
        p1.add(new JButton("Button 2"));
        p1.add(new JButton("Button 3"));

        // 创建另一个panel及其组件
        JPanel p2 = new JPanel();
        p2.add(new JTextField("TextField", 20));

        // 把两个panel加到cards中
        cards.add("Panel with Buttons", p1);
        cards.add("Panel with TextField", p2);

        // 将cards放入Frame中
        add("Center", cards);
    }

    // 响应点击切换卡片按钮的事件
    public void actionPerformed(ActionEvent e) {
        CLayout.next(cards);  // 显示下一张卡片
    }

    public static void main(String args[]) {
        CardLayoutWindow window = new CardLayoutWindow();
        window.setTitle("CardWindow Application");
        window.pack();
        window.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        window.setVisible(true);
    }
}
```

### 8.7 GridBagLayout

**特点**：不需要组件的尺寸一致，容许组件扩展到多行、多列。功能最灵活也最复杂。

### 8.8 BoxLayout

**特点**：将组件垂直摆放在一列或水平摆放在一行中。Swing新增。

### 8.9 如何选择布局管理器

| 场景 | 推荐布局 |
|------|----------|
| 组件以自然大小紧凑地在一行中显示 | **FlowLayout** |
| 组件大小相同，并且成行或成列显示 | **GridLayout** |
| 组件尽量充满容器空间 | **BorderLayout** 或 **GridBagLayout** |

---

## 九、GUI设计完整步骤

1. 引入Swing包及其它程序包
2. 选择GUI的外观风格L&F
3. 创建并设置顶层容器
4. 创建与添加Swing组件
5. 显示顶层容器，将整个GUI显示出来
6. **增加事件处理**

---

## 十、AWT事件处理模型

### 10.1 事件处理模型概述

当用户在界面上利用鼠标或键盘进行操作时，监测GUI的操作系统将所发生的事件传送给GUI应用程序，应用程序根据事件的类型作出相应的反应。

**基于Swing的GUI中，仍然采用AWT的事件处理模型**。

### 10.2 事件处理中的3类对象

| 对象 | 说明 |
|------|------|
| **事件（Event）** | 描述所发生事件的对象。Java中有很多不同类型的事件类，用来描述不同类型的用户动作（如按下键盘、拖动或单击鼠标） |
| **事件源（Event Source）** | 产生事件的组件。例如当在一个Button上单击鼠标时产生ActionEvent，这个Button就是事件源 |
| **事件处理器（Event Handler）** | 一个接收事件对象并处理用户交互的方法 |

### 10.3 委托模型（Delegation Model / 监听器模型）

```
  Frame
    └── Panel
          └── Button ──单击──→ ActionEvent
                                  │
                    ┌─────────────┴─────────────┐
                    ▼                           ▼
              监听器1（事件处理器）          监听器2（事件处理器）
         actionPerformed(ActionEvent e)  actionPerformed(ActionEvent e)
```

**核心机制**：
- 需要响应用户操作的组件事先已经注册一个或多个包含事件处理器的对象，称为**监听器（Listener）**
- 每类事件有一个相应的**监听器接口**，定义了接收事件的方法
- 实现该接口的类，其对象可作为监听器注册
- 当界面操作事件产生并被发送到产生事件的组件时，该组件将把事件发送给能接收和处理该事件的监听器
- **事件对象只向已注册的监听器报告**

### 10.4 委托方式事件处理的实现步骤

**步骤1**：在事件处理类的声明中指定要实现的监听器接口，并实现该接口中的所有方法：
```java
public class MyClass implements ActionListener {
    public void actionPerformed(ActionEvent e) {
        // ...响应某个动作的代码...
    }
}
```

**步骤2**：在一个或多个组件上将监听器类的实例注册为监听器：
```java
someComponent.addActionListener(new MyClass());
```

### 10.5 完整事件处理示例

```java
import java.awt.*;
import java.awt.event.*;
import javax.swing.*;

public class TestButton {
    public static void main(String args[]) {
        JFrame f = new JFrame("Test");
        f.setSize(200, 100);
        f.setLayout(new FlowLayout(FlowLayout.CENTER));
        f.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        JButton b = new JButton("Press Me!");
        b.addActionListener(new ButtonHandler());  // 注册监听器
        f.add(b);
        f.setVisible(true);
    }
}

// 定义ActionEvent监听器类
class ButtonHandler implements ActionListener {
    public void actionPerformed(ActionEvent e) {
        System.out.println("Action occurred");
        System.out.println("Button's label is:" + e.getActionCommand());
    }
}
```

---

## 十一、事件分类与监听器接口

### 11.1 AWT事件类层次

```
java.util.EventObject
  └── java.awt.AWTEvent
        ├── ActionEvent          （动作事件）
        ├── AdjustmentEvent      （调整事件）
        ├── ComponentEvent       （组件事件）
        │     ├── FocusEvent     （聚焦事件）
        │     ├── InputEvent     （输入事件）
        │     │     ├── KeyEvent （键盘事件）
        │     │     └── MouseEvent（鼠标事件）
        │     └── WindowEvent    （窗口事件）
        ├── ItemEvent            （条目事件）
        └── TextEvent            （文本事件）
```

### 11.2 事件接口与方法目录

| 监听器接口 | 适配器类 | 方法 |
|-----------|----------|------|
| **ActionListener** | （无） | `actionPerformed` |
| **AdjustmentListener** | （无） | `adjustmentValueChanged` |
| **ComponentListener** | ComponentAdapter | `componentHidden`, `componentMoved`, `componentResized`, `componentShown` |
| **ContainerListener** | ContainerAdapter | `componentAdded`, `componentRemoved` |
| **FocusListener** | FocusAdapter | `focusGained`, `focusLost` |
| **ItemListener** | （无） | `itemStateChanged` |
| **KeyListener** | KeyAdapter | `keyPressed`, `keyReleased`, `keyTyped` |
| **MouseListener** | MouseAdapter | `mouseClicked`, `mouseEntered`, `mouseExited`, `mousePressed`, `mouseReleased` |
| **MouseMotionListener** | MouseMotionAdapter | `mouseDragged`, `mouseMoved` |
| **TextListener** | （无） | `textValueChanged` |
| **WindowListener** | WindowAdapter | `windowActivated`, `windowClosed`, `windowClosing`, `windowDeactivated`, `windowDeiconified`, `windowIconified`, `windowOpened` |

### 11.3 多监听器

- 可以使用一个监听器处理多个组件可能产生的**多种事件**（实现多个接口）：
```java
public class ComplexListener implements
        MouseMotionListener, MouseListener, ActionListener {
    // ...
}
```
- 可以在一个组件上注册**多个监听器**，多个监听器可以监听同一个事件
- 根据需要多次调用 `addXXXListener()` 方法注册多个监听器

**示例——带有两个监听器的Frame**：
```java
public class TwoListener implements MouseMotionListener, MouseListener {
    JFrame f;
    JTextField tf;

    public void go() {
        f = new JFrame("Two Listener");
        tf = new JTextField(30);
        f.add(tf, "BorderLayout.SOUTH");
        f.addMouseMotionListener(this);
        f.addMouseListener(this);
        // ...
    }

    // MouseMotionListener events
    public void mouseDragged(MouseEvent e) { /* ... */ }
    public void mouseMoved(MouseEvent e)   { /* ... */ }

    // MouseListener events
    public void mouseEntered(MouseEvent e) {
        String s = "The mouse entered";
        tf.setText(s);
    }
    public void mouseClicked(MouseEvent e)  { /* ... */ }
    public void mouseReleased(MouseEvent e) { /* ... */ }
    // ...
}
```

---

## 十二、事件适配器（Adapter）

### 12.1 适配器的概念

- Adapter类实现了相应Listener接口，但**所有方法体都是空的**
- 用户可以把监听器类声明为Adapter类的子类，便可以**只重写需要的方法**，而不必实现接口中的所有方法

**示例**：
```java
public class MouseClickHandler extends MouseAdapter {
    public void mouseClicked(MouseEvent e) {
        // 只重写需要的方法
    }
}
```

**限制**：用户定义的监听器类只能继承一个适配器类，并且一旦从适配器继承就**不能再继承其它类**（Java单继承）。

### 12.2 使用内部类进行事件处理

既使用Adapter类，又避免多重继承的限制：在一个类中定义**内部类**，由内部类继承相应Adapter类。

**示例**：
```java
public class MyApplet extends Applet {
    // ...
    someObject.addMouseListener(new MyAdapter());

    class MyAdapter extends MouseAdapter {
        public void mouseClicked(MouseEvent e) {
            // ...Event handler implementation goes here...
        }
    }
}
```

### 12.3 使用匿名类进行事件处理

**匿名类（Anonymous Class）** 是没有名字的内部类，可以使用匿名类进行事件处理。

**示例**：
```java
public class MyApplet extends Applet {
    // ...
    someObject.addMouseListener(new MouseAdapter() {
        public void mouseClicked(MouseEvent e) {
            // ...Event handler implementation goes here...
        }
    });
}
```

---

## 十三、Swing组件分类

### 13.1 组件分类总览

| 分类 | 说明 | 示例组件 |
|------|------|----------|
| **顶层容器** | 提供绘制和事件处理基础设施 | JFrame、JApplet、JDialog |
| **通用容器** | 具有普遍应用场合的中间层容器 | JPanel、JScrollPane、JSplitPane、JToolBar、JTabbedPane |
| **特殊容器** | 起特殊作用的中间层容器 | JInternalFrame、JLayeredPane、JRootPane |
| **基本控制组件** | 主要用于接收用户输入，显示简单状态 | JButton、JMenu、JRadioButton、JCheckBox |
| **不可编辑的信息显示** | 完全用来显示信息 | JLabel、JProgressBar |
| **可编辑的信息显示** | 显示可被编辑的信息 | JTable、JTree、JTextArea、JTextField |

**原则**：一般如果使用Swing组件，则程序中**只使用Swing组件和Swing容器**。

### 13.2 对话框（JOptionPane）

JOptionPane提供四种标准对话框：

| 对话框类型 | 方法 |
|-----------|------|
| 消息对话框 | `showMessageDialog` |
| 选项对话框 | `showOptionDialog` |
| 输入对话框 | `showInputDialog` |
| 确认对话框 | `showConfirmDialog` |

### 13.3 按钮类常用组件

| 组件 | 说明 |
|------|------|
| **JButton** | 普通按钮 |
| **JRadioButton** | 单选按钮 |
| **JCheckBox** | 复选框 |
| **JToggleButton** | 开关按钮 |

### 13.4 文本类常用组件

| 组件 | 说明 |
|------|------|
| **JTextField** | 单行文本框 |
| **JPasswordField** | 密码输入框 |
| **JTextArea** | 多行文本区 |
| **JEditorPane** | 编辑器面板 |
| **JTextPane** | 文本面板 |

### 13.5 其他常用组件

| 组件 | 说明 |
|------|------|
| **JLabel** | 标签（显示文本/图标） |
| **JList** | 列表 |
| **JTable** | 表格 |
| **JTree** | 树形结构 |
| **JComboBox** | 组合框（下拉列表） |
| **JFileChooser** | 文件选择器 |
| **JMenu** | 菜单 |
| **JColorChooser** | 颜色选择器 |
| **JProgressBar** | 进度条 |
| **JSlider** | 滑块 |
| **JSpinner** | 微调器 |

---

## 十四、零基础补充：GUI 程序从零理解

### 14.1 GUI 程序和控制台程序的区别

| 类型 | 交互方式 | 程序特点 |
|------|----------|----------|
| 控制台程序 | 键盘输入、文字输出 | 通常从 `main` 开始顺序执行 |
| GUI 程序 | 窗口、按钮、文本框、鼠标键盘事件 | 程序显示界面后等待用户操作 |

GUI 程序不是一直按顺序往下跑，而是“事件驱动”：用户点按钮、输入文字、关闭窗口时，程序再调用对应处理代码。

### 14.2 Swing GUI 创建流程完整图

```text
导入 Swing/AWT 包
    ↓
选择 Look & Feel
    ↓
创建顶层容器 JFrame/JDialog
    ↓
设置布局管理器
    ↓
创建并添加组件 JButton/JLabel/JTextField 等
    ↓
注册事件监听器
    ↓
显示窗口 setVisible(true)
```

最小窗口示例：

```java
import javax.swing.*;

public class HelloFrame {
    public static void main(String[] args) {
        JFrame frame = new JFrame("Hello");
        frame.add(new JLabel("Hello Swing"));
        frame.setSize(300, 200);
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.setVisible(true);
    }
}
```

### 14.3 顶层容器、中间容器、基本组件

| 类型 | 作用 | 例子 |
|------|------|------|
| 顶层容器 | 一个窗口的根 | `JFrame`、`JDialog` |
| 中间容器 | 帮助组织组件 | `JPanel`、`JScrollPane`、`JSplitPane` |
| 基本组件 | 用户直接看到或操作 | `JButton`、`JLabel`、`JTextField`、`JTable` |

向 `JFrame` 添加组件时，本质上是加到内容面板上：

```java
frame.getContentPane().add(new JButton("确定"));
```

JDK 1.5 以后可以简写为：

```java
frame.add(new JButton("确定"));
```

### 14.4 事件处理模型图示说明

```text
用户操作
  例如点击按钮
      ↓
事件源
  JButton
      ↓ 产生 ActionEvent
监听器
  ActionListener
      ↓
事件处理方法
  actionPerformed(...)
```

示例：

```java
button.addActionListener(e -> {
    System.out.println("按钮被点击");
});
```

### 14.5 常见 Swing 组件速查

| 类别 | 常见组件 |
|------|----------|
| 对话框 | `JOptionPane` |
| 通用容器 | `JScrollPane`、`JSplitPane`、`JTabbedPane`、`JToolBar` |
| 特殊容器 | `JInternalFrame`、`JLayeredPane`、`JRootPane` |
| 按钮类 | `JButton`、`JCheckBox`、`JRadioButton` |
| 文本类 | `JTextField`、`JPasswordField`、`JTextArea`、`JEditorPane`、`JTextPane` |
| 其他常用 | `JComboBox`、`JLabel`、`JList`、`JTable`、`JTree`、`JMenu`、`JFileChooser` |
| 其他组件 | `JProgressBar`、`JSlider`、`JColorChooser`、`JSpinner` |

### 14.6 初学者写 GUI 的常见错误

1. 创建了窗口但忘记 `setVisible(true)`。
2. 添加组件后布局不符合预期，没有理解布局管理器。
3. 按钮点击没反应，因为没有注册监听器。
4. 在事件处理方法里写耗时任务，导致界面卡住。
5. 混淆 AWT 和 Swing 组件，例如 `Button` 与 `JButton`。

## 自动查漏补充

- Swing 支持设置边框和使用图标，这也是它比早期 AWT 更适合构建现代界面的原因之一。
- 基本组件是直接向用户展示信息或获取用户输入的组件，例如按钮、标签、文本框、表格等。
- JDK 1.5 以后，`JFrame.add(child)` 会自动把组件转交给内容面板，不必总是手动写 `getContentPane().add(child)`。
- 常见事件包括条目状态改变、文本域或文本区值改变，以及窗口激活、去活、打开、关闭、最小化、从图标恢复等窗口事件。
- 使用事件适配器可以少写无关方法，但由于 Java 单继承限制，如果类已经继承了适配器，就不能再继承其他类。

## PDF逐页知识点补全索引

这一节按原 PDF 页码补全知识点，用来兜底检查前文是否遗漏。零基础学习时，可以先读前文讲解，再用这里逐页核对。

### 第 1 页：图形化用户界面
- Java GUI概述
- 基于Swing的GUI构造方法
- AWT事件处理模型
- Swing组件

### 第 2 页：Java基础类库
- JFC(Java Foundation Classes)：Java基础类库，包括开发GUI所需
- 的组件和服务，包含5个部分的API：AWT、Java 2D、Accessibility、
- Drag ＆ Drop、Swing。

### 第 3 页：Java基础类库
- AWT(Abstract Window Toolkit)是抽象窗口工具集，包括建立GUI的
- 各种组件与事件处理机制。
- Java 2D是一套图形API，它为Java应用程序提供了一套高级的有关二
- 维图形图像处理的类。Java2D API扩展了java.awt和java.awt.image类，
- 并提供了丰富的绘图风格，定义了复杂图形的机制和精心调节绘制过
- 程的方法和类。
- Accessibility提供了一套高级工具，用以辅助开发使用非传统输入和输
- 出的应用程序。它提供了一个辅助的技术接口，如屏幕阅读器、屏幕

### 第 4 页：抽象窗口工具集AWT简介
- 设计目标是具有独立于平台的API，但同时保留每个平台
- 的界面显示风格L&F (Look & Feel) 。
- 实现方法是：定义各种组件（Components）类平台独立
- 的API，然后利用特定于平台的各种类的实现（称为对等
- 组件，peers）提供具有特定平台风格的L&F。
- 平台无关的API
- 特定于平台的实现
- 和实现

### 第 5 页：AWT组件
- java.awt包括建立GUI所需基本组件。
- java.awt的主要类及层次关系
- AWTEvent
- Object
- FlowLayout
- BorderLayout
- GridBagLayout
- MenuBar

### 第 6 页：Swing简介
- Swing中的所有组件全部用Java语言实现（轻量级）。
- 采用分离模型：组件及与组件相关的数据模型。
- 可设置的组件外观感觉（Look & Feel）。
- 与AWT相比提供更丰富的GUI组件，引入新的特征，
- 并提供更丰富的功能。例如：
- 高层组件集合，如表格JTable、树JTree；
- 都实现了Accessible接口；
- 支持键盘代替鼠标；

### 第 7 页：Swing组件类层次
- java.awt.Container
- Window
- Panel
- javax.swing.JComponent
- JTextArea
- JTextComponent JTextField JPasswordField
- JEditPane
- JCheckBox

### 第 8 页：基于Swing的GUI设计步骤
- import javax.swing.*;
-  引入Swing 包及其它程序包
- public class HelloWorldSwing {
-  选择GUI的外观风格L&F
- public static void main(String[] args) {
-  创建并设置窗口容器 JFrame frame = new JFrame("HelloWorldSwing");
- final JLabel label = new JLabel("Hello World!");
-  创建与添加Swing组件

### 第 9 页：Swing中的Look & Feel
- Java L&F
- CDE/Motif L&F Windows L&F

### 第 10 页：Swing中的Look & Feel
- 使用UIManager.setLookAndFeel( ) 方法进行设置。
- 在该方法中可以使用的参数：
- UIManager.getCrossPlatformLookAndFeelClassName()
- Java Look & Feel
- UIManager.getSystemLookAndFeelClassName()
- 当前平台的Look & Feel
- "javax.swing.plaf.metal.MetalLookAndFeel"
- "com.sun.java.swing.plaf.windows.WindowsLookAndFeel"

### 第 11 页：常用容器
- 组成GUI的组件如按钮、标签等，不能独立使用，
- 必须放在容器内。
- 容器本身也是一个组件，具有组件的所有性质，
- 另外还具有容纳其它组件的功能。
- 所有的组件都可以通过add()方法向容器中添加。
- 在Swing中，常用的三种类型的容器是JFrame、
- JPanel、JApplet。

### 第 12 页：顶层容器
- Swing GUI形成顶层容器-中间层容器-基本组件的
- 层次包容关系。
- 具有Swing GUI的应用必须至少有一个顶层容器，
- 它提供了所包含组件需要的绘制与事件处理功能。
- 对于多数应用，顶层容器是JFrame、JDialog或
- JApplet的实例。
- 中间层容器是由通用容器构成，主要是为了简化组
- 件的布局，常用组件为JPanel、JScrollPane、

### 第 13 页：向顶层容器中加入组件
- 对JFrame添加组件
- 用getContentPane()方法获得JFrame的内容面板，再使用add()
- 方法向其中加入组件。
- frame.getContentPane().add(new JLabel("Hello!"));
- 建立一个JPanel等中间容器，把组件添加到中间容器中，再用
- setContentPane()方法把该容器设置为JFrame的内容面板。
- JPanel contentPane = new JPanel();
- JButton b = new JButton("确定");

### 第 14 页：JFrame
- 最常用的顶层容器，一般作为Java Application的主窗口。
- import java.awt.*;
- import javax.swing.*;
- public class JFrameDemo {
- public static void main(String s[]) {
- // 指定使用当前的Look&Feel装饰窗口。必须在创建窗口前设定。
- JFrame.setDefaultLookAndFeelDecorated(true);
- // 创建并设定关闭窗口操作。

### 第 15 页：通用容器JPanel
- JPanel是存放轻型组件的通用容器，缺省情况下是透明的，其对象可
- 作为顶层容器的Content Pane使用。
- import java.awt.*;
- import javax.swing.*;
- public class FrameWithPanel extends JFrame {
- public FrameWithPanel(String str) {
- super(str);
- public static void main(String args[]) {

### 第 16 页：Layout Manager（布局管理器）
- 为了实现跨平台的特性并获得动态的布局效果，Java在容器
- （如JFrame, JPanel）设置了布局管理器（Layout Manager）
- 负责对容器内的组件进行布局。

### 第 17 页：Layout Manager
- 容器中组件的布局通常由Layout Manager控制。
- Layout Manager负责决定容器的布局策略及容器内组件
- 的排列顺序、大小和位置，以及当窗口移动或调整大小后
- 组件如何变化等。
- 每个容器都有一个默认的Layout Manager，可通过调用
- setLayout()方法改变。
- Java提供的布局管理器
- FlowLayout 流式

### 第 18 页：FlowLayout Manager
- 组件采用从左到右，从上到下逐行摆放。
- Panel和Applet默认布局
- Flow Layout
- Open Close
- Open Close ok
- ok
- setLayout(new FlowLayout(int align, int hgap, int vgap))
- FlowLayout.LEFT

### 第 19 页：FlowLayout Manager
- 例：import java.awt.*;
- import javax.swing.*;
- public class FlowLayoutWindow extends JFrame {
- public FlowLayoutWindow() {
- setLayout(new FlowLayout());
- add(new JLabel("Buttons:"));
- add(new JButton("Button 1"));
- add(new JButton("2"));

### 第 20 页：BorderLayout Manager
- BorderLayout 分5个区：
- Window, Dialog和Frame默认布局
- BorderLayout
- North
- West Center East
- South
- setLayout(new BorderLayout(int hgap, int vgap))
- 加入组件：add(new JButton("Center"),"Center")

### 第 21 页：BorderLayout Manager
- import java.awt.*;
- import javax.swing.*;
- public class BorderLayoutWindow extends JFrame {
- public BorderLayoutWindow() {
- setLayout(new BorderLayout());
- add(new JButton("North"), "North");
- add( new JButton("South"),"South");
- add( new JButton("East"),"East");

### 第 22 页：GridLayout Manager
- 把容器分成n行*m列同样大小的网格单元，每个单元可
- 充满一个组件。
- 组件从左到右、从上到下填充。
- 构造与安装布局管理器：
- setLayout(new GridLayout(int rows, int cols, int hgap,
- int vgap))
- rows或cols可取0，表示任意数目的行或列。
- 先满足行的限制。

### 第 23 页：GridLayout Manager
- import java.awt.*;
- import javax.swing.*;
- public class GridLayoutWindow extends JFrame {
- public GridLayoutWindow() {
- setLayout(new GridLayout(3,2));
- add(new JButton("Button 1"));
- add(new JButton("2"));
- add(new JButton("Button 3"));

### 第 24 页：CardLayout Manager
- 把组件象一系列卡片一样叠放，一个时刻只能看到最上面的。
- setLayout(new CardLayout(int hgap, int vgap))
- public void next(Container parent)//循环显示下一张卡片
- first, previous, next, last, show
- public Component add(String name, Component comp)

### 第 25 页：CardLayout Manager
- import java.awt.*;
- import java.awt.event.*;
- import javax.swing.*;
- public class CardLayoutWindow extends JFrame implements
- ActionListener {
- JPanel cards;
- CardLayout CLayout=new CardLayout();
- public CardLayoutWindow() {

### 第 26 页：CardLayout Manager
- //创建cards中的第一个panel及其组件。
- JPanel p1 = new JPanel();
- p1.add(new JButton("Button 1"));
- p1.add(new JButton("Button 2"));
- p1.add(new JButton("Button 3"));
- //创建cards中的另一个panel及其组件。
- JPanel p2 = new JPanel();
- p2.add(new JTextField("TextField", 20));

### 第 27 页：GridBagLayout Manager
- GridBagLayout不需要组件的尺寸一致，容许组件扩展到
- 多行、多列。

### 第 28 页：BoxLayout Manager
- BoxLayout将组件垂直摆放在一列或水平摆放在一行中。

### 第 29 页：如何选择布局管理器
- 组件以自然大小紧凑的在一行中显示——FlowLayout
- 组件大小相同，并且成行或成列显示——GridLayout
- 组件尽量充满容器空间——使用BorderLayout或
- GridBagLayout

### 第 30 页：GUI的设计步骤
- 引入Swing 包及其它程序包
- 选择GUI的外观风格L&F
- 创建并设置顶层容器
- 创建与添加Swing组件
- 显示顶层容器，将整个GUI显示出来
- 增加事件处理

### 第 31 页：AWT事件处理模型
- 当用户在界面上利用鼠标或键盘进行操作时，监测
- GUI的操作系统将所发生的事件传送给GUI应用程序，
- 应用程序根据事件的类型作出相应的反应。
- 基于Swing的GUI中，仍然采用AWT的事件处理模型
- 什么是事件
- 事件处理机制
- 事件目录
- 事件、接口、方法列表

### 第 32 页：事件处理模型中的3类对象
- 事件—描述所发生事件的对象
- Java中有很多不同类型的事件类，用来描述不同类型的
- 用户动作。如按下键盘、拖动或单击鼠标。
- 事件源—产生事件的组件
- 产生事件的组件就是一个事件源。例如当在一个Button
- 上单击鼠标时，将产生一个ActionEvent事件，这个Button就
- 是事件源。
- 事件处理器—一个接收事件对象并处理用户交互的方法

### 第 33 页：事件处理机制
- 委托模型/监听器模型：
- 用户单击Button
- Frame
- 监听器1
- 事件处理器
- Panel
- actionPerformed(ActionEvent e){
- Button

### 第 34 页：事件处理机制
- 每类事件有一个相应的监听器接口，定义了接收
- 事件的方法。实现该接口的类，其对象可作为监听
- 器注册。
- 需要响应用户操作的相关组件要注册一个或多个
- 相应事件的监听器，该监听器中包含了能接收和处
- 理事件的事件处理。
- 事件对象只向已注册的监听器报告。

### 第 35 页：事件处理机制
- 委托方式的事件处理机制的实现包括以下两部分：
- 1.在事件处理类的声明中指定要实现的监听器接口，并实现
- 该监听器接口中的所有方法。例如：
- public class MyClass implements ActionListener {
- public void actionPerformed(ActionEvent e) {
- ...//响应某个动作的代码...
- 2.在一个或多个组件上将监听器类的实例注册为监听器，如：
- someComponent.addActionListener(new MyClass( ));

### 第 36 页：JFrame f = new JFrame("Test");
- import java.awt.*;
- import java.awt.event.*;
- import javax.swing.*;
- public class TestButton {
- public static void main(String args[]) {
- f.setSize(200, 100);
- f.setLayout(new FlowLayout(FlowLayout.CENTER));
- f.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);

### 第 37 页：事件分类
- java.util.EventObject
- ActionEvent
- ContainerEvent
- java.awt.AWTEvent AdjustmentEvent
- FocusEvent
- ComponentEvent
- InputEvent
- ItemEvent WindowEvent

### 第 38 页：事件监听器接口
- 对于AWT每种类型的事件，都定义了相应的事件处
- 理接口，即监听器接口。监听器接口中要定义一个或多
- 个事件处理方法，这些方法在特定事件出现时被调用。
- 实现某种监听器接口的类，其对象可以作为接收并
- 处理相应事件的监听器。
- java.util.EventListener
- ActionListener
- ItemListener

### 第 39 页：事件接口与方法目录
- Listener Interface Adapter Class Methods
- ActionListener actionPerformed
- 激活组件
- AdjustmentListener adjustmentValueChanged
- 移动滚动条
- ComponentListener ComponentAdapter componentHidden
- componentMoved
- 组件移动、缩放、显示、隐藏

### 第 40 页：事件接口与方法目录
- Listener Interface Adapter Class Methods
- KeyListener keyPressed
- KeyAdapter keyReleased
- 键盘输入
- keyTyped
- mouseClicked
- MouseListener mouseEntered
- MouseAdapter mouseExited

### 第 41 页：AWT事件处理示例
- 可以使用一个监听器处理多个组件可能产生的
- 多种事件。如：
- public class ComplexListener implements
- MouseMotionListener, MouseListener,
- ActionListener{ ……
- 可以在一个组件上注册多个监听器，而且多个
- 监听器可以监听同一个事件。
- 根据需要多次调用addXXXListener()方法注册

### 第 42 页：举例—带有两个监听器的Frame
- public class TwoListener implements
- MouseMotionListener, MouseListener{
- JFrame f;
- JTextField tf;
- public static void main(String args[]){
- TwoListener two = new TwoListener( );
- two.go();

### 第 43 页：f=new JFrame("Two Listener");
- public void go(){
- tf=new JTextField(30);
- f.add(tf,"BorderLayout.SOUTH");
- f.addMouseMotionListener(this);
- f.addMouseListener(this);
- … }
- // These are MouseMotionListener events
- public void mouseDragged(MouseEvent e){… }

### 第 44 页：事件适配器
- Adapter类实现了相应Listener接口，但所有方法体都
- 是空的。
- 用户可以把自己的监听器类声明为Adapter类的子类，
- 便可以只重写需要的方法。
- public class MouseClickHandler extends MouseAdapter{
- public void mouseClicked(MouseEvent e){
- ….
- 用户定义的监听器类只能继承一个适配器类，并且一旦

### 第 45 页：使用内部类进行事件处理
- 既使用Adapter类，又避免多重继承的限制：
- 在一个类中定义内部类，由内部类继承相应Adapter类。
- 例：
- public class MyApplet extends Applet {
- ...
- someObject.addMouseListener(new MyAdapter());
- class MyAdapter extends MouseAdapter {
- public void mouseClicked(MouseEvent e) {

### 第 46 页：使用匿名类进行事件处理
- 匿名类（Anonymous Class）是没有名字的内部类。
- 可以使用匿名类进行事件处理。
- 例：
- public class MyApplet extends Applet {
- ...
- someObject.addMouseListener(new MouseAdapter() {
- public void mouseClicked(MouseEvent e) {
- ...//Event handler implementation goes here... } });

### 第 47 页：Swing组件
- Swing组件分类：
- ■ 顶层容器：
- 主要包括JFrame、JApplet、 JDialog等
- ■ 通用容器：
- 具有普遍应用场合的中间层容器，如JPanel、JScrollPane
- ■ 特殊容器：
- 起特殊作用的中间层容器，如JIternalFrame、JLayeredPane
- ■ 基本控制组件：

### 第 48 页：Swing组件—对话框(容器类）
- JOptionPane
- showMessageDialog
- showOptionDialog
- showConfirmDialog showInputDialog

### 第 49 页：Swing组件—通用容器
- JScrollPane
- JSplitPane
- JTabbedPane
- JToolBar

### 第 50 页：Swing组件—特殊用途的容器
- JInternalFrame
- JLayeredPane
- JRootPane

### 第 51 页：Swing组件—按钮类常用组件
- JButton
- JCheckBox
- JRadioButton

### 第 52 页：Swing组件—文本类常用组件
- JTextField，JPasswordField，JTextArea，JEditorPane，JTextPane

### 第 53 页：Swing组件—其他常用组件
- JComboBox
- JLable JList
- JTable
- JTree

### 第 54 页：Swing组件—其他常用组件
- JMenu
- JFileChooser

### 第 55 页：Swing组件—其他组件
- JProgressBar
- JSlide
- JColorChooser
- JSpinner

## 十五、本章小结

本章系统介绍了Java图形化用户界面（GUI）编程，涵盖以下核心知识点：

1. **Java基础类库（JFC）**：包含AWT、Java 2D、Accessibility、Drag & Drop、Swing五个部分，Swing是重点

2. **AWT**：抽象窗口工具集，定义平台独立的组件API，通过平台特定的对等组件（Peers）实现各平台L&F

3. **Swing**：全部用Java实现的轻量级组件，具有分离模型、可设置L&F、比AWT更丰富的组件（JTable、JTree等）

4. **GUI设计步骤**：引入包 → 选择L&F → 创建顶层容器 → 添加Swing组件 → 显示 → 增加事件处理

5. **容器层次**：顶层容器（JFrame）→ 中间层容器（JPanel）→ 基本组件，通过add()方法组装

6. **布局管理器**：
   - FlowLayout：从左到右逐行（Panel默认）
   - BorderLayout：5个方向分区（Frame/Window默认）
   - GridLayout：均匀网格
   - CardLayout：卡片叠放，一次显示一张
   - GridBagLayout：灵活网格，组件可跨行跨列
   - BoxLayout：垂直或水平排列

7. **AWT事件处理模型**：委托模型/监听器模型——事件源产生事件，已注册的监听器接收并处理事件

8. **事件体系**：ActionEvent、ComponentEvent（含FocusEvent/InputEvent/KeyEvent/MouseEvent/WindowEvent）、ItemEvent等

9. **监听器接口与适配器**：实现Listener接口（需实现所有方法）vs 继承Adapter类（只重写需要的方法，但受单继承限制）

10. **内部类与匿名类**：用于事件处理，内部类继承Adapter避免多重继承限制；匿名类写法更简洁

11. **Swing组件**：顶层容器、通用容器、特殊容器、基本控制、不可编辑显示、可编辑显示六大类，涵盖JButton/JTextField/JTable/JTree/JFileChooser等
